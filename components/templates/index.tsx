import { Base } from "./Base";
import { Form } from "./Form";
import { Normal } from "./Normal";
import { Sub } from "./Sub";

const Layout = {
  Base,
  Form,
  Normal,
  Sub,
};
export default Layout;
